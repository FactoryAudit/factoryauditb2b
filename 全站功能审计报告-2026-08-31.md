# FactoryAuditB2B 全站空功能 / 假功能审计报告

**日期**：2026-08-31
**范围**：全站 40+ 路由、7 个 API、15 个组件、9 种语言
**方法**：3 组并行代码审计 + 端到端业务流真实跑测（非 mock）

---

## 一、审计结论速览

| 严重度 | 数量 | 状态 |
|---|---|---|
| 严重（功能为空 / 结果造假 / 主路径断裂） | 6 | 全部修复 |
| 中等（体验断裂 / 误导） | 5 | 全部修复 |
| 轻微（死代码 / 孤儿页） | 4 | 全部修复 |

**全站无 `href="#"` 死链**（此前已清理）。剩余缺口见第五节，均为外部依赖未配置，非代码缺陷。

---

## 二、修复的严重问题

### 1. 完全没有注册功能 ← 最严重

**问题**：全项目只有 `/login`，无任何注册入口与 API。`User / Account / Session` 三张表形同虚设，真实访客无法建立账号，唯一用户来源是 seed 里的 3 个硬编码账号。

**修复**：
- `app/api/register/route.ts`（新）：bcrypt(10) 加密、邮箱查重、角色白名单
- `app/[locale]/register/page.tsx`（新）：9 语言注册页
- `components/RegisterForm.tsx`（新）：买家/供应商角色卡片选择、双密码确认、注册成功后自动登录

**安全设计**：
- 只开放 `BUYER` / `SUPPLIER` 自助注册，后端白名单拦截 `ADMIN` / `AUDITOR`（防越权提权）
- 密码长度 8–200 字符（下限防弱密码，上限防 bcrypt 超长串 DoS）
- 注册同时生成一条 `tool=register` 的线索（B2B 注册 = 高意向买家/供应商）

### 2. 后台线索审核流程完全不存在

**问题**：`/admin/leads` 的 Status 只是纯展示，全项目 7 个 API 路由中没有 lead 更新路由。线索写入后**永远停在 `NEW`**，`NEW → CONTACTED → QUALIFIED → WON` 全链路断裂。

**修复**：
- `app/api/admin/leads/route.ts`（新）：PATCH + `requireAdmin` 鉴权 + status 白名单
- `components/admin/LeadStatusSelect.tsx`（新）：下拉实时写库、乐观更新、失败自动回滚
- 后台页补 Score / Sourcing 列、状态分布统计、ISO 时间格式

**关键**：`Lead.status` 在 SQLite 下是 `String` 不是 enum，服务端必须做白名单校验，否则可写入任意值。

### 3. 验厂报告分析器结果造假

**问题**：`lib/ai.ts` 的 `aiReportReview` 在无 `DEEPSEEK_API_KEY` 时用 `Math.min(100, report.length / 10)` 评分——**分数只跟字数有关**。粘贴 1000 字乱码得到「Report Quality 100/100 + No obvious missing items detected」，而页面宣称"check structure, missing items and risk"。

**修复**：新增 `localReportReview()`，改为内容驱动：
- **10 个必备章节**关键词匹配加权（工厂信息 / 审计日期 / 审核员 / 范围标准 / 发现项分级 / 纠正措施 / 证据 / 员工人数 / 结束会议 / 方法论）
- **5 项交叉一致性校验**：有 critical 无纠正措施、声称无发现却列 NC、有纠正措施无截止日、审计日期在未来、含模板占位符

**实测对比**：

| 输入 | 修复前 | 修复后 |
|---|---|---|
| 乱码 1680 字 | **100/100** | **0/100**（10 项缺失） |
| 过短文本 12 字 | — | 0/100（提示最少 80 字） |
| 模板占位符 | — | 28/100（识别 TBD/TODO） |
| 真实完整报告 | — | **100/100**（0 问题） |
| 有 critical 无纠正措施 | — | **66/100** + 报不一致 |

### 4. 多语言链接丢失前缀（主转化路径）

| 位置 | 问题 |
|---|---|
| `pricing/page.tsx:83` | `href={plan.href}` 未走 `lp()`，中文站点「申请报价」被丢到英文页 |
| `AuthMenu.tsx` | `/login`、`/admin`、`signOut callbackUrl` 全丢前缀 |
| `login/page.tsx` | 不接 `searchParams`，`?error=admin_only` 提示永不显示 |
| `login/page.tsx` | `<Link href="/">` 丢前缀 |

**修复**：全部改用 `localePath()`；login 页接 `searchParams` 显示对应语言错误提示，并支持 `next` 参数回跳（防 open redirect，只接受站内相对路径）。

### 5. 行业页 WRAP 标签指向 404

**问题**：`getSeoMatrix()` 按 `isAudit: true` 过滤（只有 SMETA / BSCI / ICTI / RBA），但 `SupplierCapability` 表里挂着 `WRAP`（`isAudit: false`）。`/industry/textiles` 渲染出 `/audit-guide/china/WRAP` → **404**。

**修复**：渲染前用 `validAuditCodes` Set 过滤；行业页审核类型链接取该行业供应商最集中的国家，无供应商时回退 `china`。

### 6. audit-guide 页整页硬编码中文

**问题**：英文站 `/audit-guide/china/SMETA` 显示中文正文（「已通过 SMETA 的供应商」「风险」等）；`generateStaticParams` 漏了 `locale`；JSON-LD 面包屑指向不存在的 `/audit-guide` 与 `/audit-guide/{code}` 两层。

**修复**：整页重写走字典（`auditGuide` 命名空间，9 语言）、补 `locale` 到 `generateStaticParams`、面包屑只保留真实存在的层级。

---

## 三、修复的中等 / 轻微问题

| # | 问题 | 修复 |
|---|---|---|
| 7 | pricing 用 `plan.name === "Supplier Verification"` 判断高亮，中文站永不匹配 | 改用数组索引 `RECOMMENDED_PLAN_INDEX = 1` |
| 8 | `supplier-risk-assessment` 页面暴露调试用原始 JSON | 移除 |
| 9 | `/admin` 缺 taxonomy 入口（孤儿页，建好了但点不到） | 补上并加说明文案 |
| 10 | 行业页 4 个审核类型标签全部指向同一个通用页 | 改为指向对应 audit-guide |
| 11 | 行业页供应商列表硬编码「风险」 | 走字典 `industryPage.riskLabel` |
| 12 | 后台时间用 `toLocaleString("zh-CN")` 硬编码 | 改 ISO 格式（避免水合不一致） |
| 13 | 后台 leads 页缺 Score / Sourcing 列 | 补上 |
| 14 | `pricing` 无推荐标签 | 新增 `recommendedTag`（9 语言） |

---

## 四、端到端业务流实测（真实落库，全部通过）

### Step 1 — 注册

| 用例 | 期望 | 结果 |
|---|---|---|
| 正常注册（BUYER） | 200 | `{"ok":true,"role":"BUYER"}` |
| 重复邮箱 | 409 | `email_taken` |
| 弱密码（3 位） | 400 | `weak_password` |
| 非法邮箱 | 400 | `invalid_email` |
| **越权提权 ADMIN** | 400 | `invalid_role`（已拦截） |

数据库验证：`passwordHash` 为 bcrypt（`$2b$10$...`），无明文；同时自动生成 `tool=register, score=70` 的线索。

### Step 2 — 登录

新注册用户 credentials 登录成功，`session` 返回 `{"role":"BUYER"}`。

### Step 3 — 提交业务申请（4 类全部落库）

| 申请类型 | tool | score | 落库 |
|---|---|---|---|
| 风险评估 | `supplier-risk-calculator` | 82 | ✓ 含关联 Assessment（overall=42, level=HIGH, dimensions） |
| RFQ 询价 | `rfq` | 70 | ✓ |
| 验厂申请 | `audit-request` | 70 | ✓ |
| 验货申请 | `inspection-request` | 70 | ✓ |
| 缺 email | — | — | 400（正确拒绝） |

### Step 4 — 权限验证

- 未登录 PATCH → **403**
- 普通买家登录态 PATCH → **403**
- 管理员登录 → `role: ADMIN`

### Step 5 — 审核状态流转

```
NEW → CONTACTED → QUALIFIED → QUOTE_SENT   全部成功写入
NEW → WON                                  成功
NEW → LOST                                 成功
非法状态 "HACKED"                           400 + 返回白名单
不存在的线索                                404
缺 id                                      400
```

### Step 6 — 邮件通知

SMTP 连通性 OK，邮件被接受并返回 `messageId`。

### Step 7 — 供应商路径

供应商注册 → 登录成功（`role: SUPPLIER`）→ 访问 `/admin` 被 **307 拦截**。

### Step 8 — 清理

测试用户与线索全部删除，数据库回到 3 用户 / 10 线索初始状态。

---

## 五、仍存在的已知缺口（未修，诚实告知）

| 缺口 | 影响 | 建议 |
|---|---|---|
| **SMTP 是 Ethereal 测试账号** | 邮件能被 SMTP 接受，但**不会投递到真实收件箱**。客户收不到确认邮件、管理员收不到线索通知 | **上线前必须换真实 SMTP**（这是上线前 4 大阻塞之一，仍未解决） |
| Google OAuth 未配置 | `GOOGLE_CLIENT_ID/SECRET` 为空，provider 自动不启用，Credentials 登录正常 | 需要时配置即可，非阻塞 |
| `/api/lead` 无鉴权无限流 | 可被循环 POST 灌垃圾线索，每次触发两封邮件 | 上线前加基础限流（如 IP + 时间窗） |
| MyMemory 翻译配额受限 | 新增字典键靠手写 | 配额恢复后跑 `sync-dict.cjs` 补齐 |

---

## 六、验证结果

- `tsc --noEmit`：**0 错误**
- `next build`：**通过**
- 全站路由冒烟：**ALL ROUTES 200**（含 sitemap.xml / llms.txt / robots.txt）
- 9 语言注册 / 登录页均正常渲染
