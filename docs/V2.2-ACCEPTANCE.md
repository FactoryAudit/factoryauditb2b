# FactoryAuditB2B V2.2 — Acceptance Report
## Commercial Rules Unification + SEO Canonical Cleanup

**Date:** 2026-09-17
**Scope:** Incremental upgrade of factoryauditb2b.com (NOT a rewrite)
**Status legend:** ✅ REAL · 🟡 PARTIAL · 🔲 MOCK · ⬜ TODO

---

## 1. Commercial Rules Unification

### 1.1 Single pricing truth source — ✅ REAL
- `lib/commercialConfig.ts` (NEW) is the public display truth: Free / $99 Founding Buyer / $99–129 Verification / $399+ Factory Audit / **$199+ Inspection** / 3–5% Sourcing / Monitoring annual / Supplier Improvement custom.
- `lib/commerce.ts` stays the order-amount truth (USD cents, server only). Inspection `unitAmountMinor` corrected **9900 → 19900** (V2.2 §7/§36). Historical orders' `amount_minor` is immutable (spec §67/§68) — no backfill needed.
- `scripts/cs17-commerce-regression.ts` assertion `=== 9900` updated to `=== 19900` (would otherwise fail).
- Regression `[1]`+`[2]` verify both sources agree (52→61 checks PASS).

### 1.2 Inspection price = $199 — ✅ REAL (all 9 locales)
- `pricing.reports` range = `USD 199+` in en/zh/zh-TW/de/fr/es/pt/ja/ar (`[3]`).
- `/pricing` `metaDesc` inspection clause = $199 in all 9 (`[3]`).
- **`/services/inspection` page body** (`inspection.pricingLead`) now states "From USD 199 / man-day" in all 9 (was EN-only; zh/zh-TW/de/fr/es/pt/ja/ar added — `[3b]`).
- Verification $99–129 preserved everywhere (§38).

### 1.3 Founding Buyer naming unification — ✅ REAL
- `/membership` page merged into `/pricing` as the **Founding Buyer** option (anchor `#founding-buyer`).
- Legacy "Founder Buyer" in supplier-page copy (`unlockLead`/`unlockNote`/`founderCta`) across all 9 dicts unified to **Founding Buyer** (the site's own `membership` object already branded it "Founding Buyer"; the unlock copy was the drift). 0 "Founder Buyer" remaining in shipped dicts.
- `lib/analytics.ts`: `membership_page_view` / `membership_cta` retired; Founding Buyer covered by `founding_buyer_view` / `founding_buyer_checkout_start` / `founding_buyer_purchase` funnel (`[7]`).

### 1.4 Risk model → 8 dimensions — ✅ REAL
- `lib/riskEngine.ts` algorithm was **already 8-dimension** (company/quality/compliance/production/supplychain/documentation/certification/digitalFootprint, weights 12/16/16/12/12/12/10/10). Only dict copy needed fixing.
- Removed all "10-dimension" / "ten dimension" / "dez dimensões" / "diez dimensiones" / "عشرة أبعاد" / "十個維度" / "10 個維度" / "10 次元" phrasing from en/zh/zh-TW/de/fr/es/pt/ja/ar `riskScore` label + `riskLead` enumeration (`[3]`).
- `lib/supplierNetwork.ts` (auditor **internal** 10-dim model + "Verified Supplier" status) retained as designed — it is a separate internal assessment, not the public "Supplier Risk Assessment". No public surfacing of "Verified Supplier" (regression `[6]`).

### 1.5 Audit Type ≠ Risk Level — ✅ REAL
- `lib/auditScope.ts` `AUDIT_TYPES` (audit categories) and `announced/semi-announced/unannounced` (execution method, in `api/audit/request`, `components/AuditRequestForm`, `lib/audits`, `supabase/cs18`) remain correctly layered. No change required (verified in V2.2 §6 scan).

---

## 2. Page-by-page changes (8 page types)

| Page | Change | Status |
| --- | --- | --- |
| `/pricing` | Added `#founding-buyer` section (h1/lead/price/benefits×4/CheckoutButton/paymentNote) reusing `t.membership`; single truth source | ✅ REAL |
| `/membership` | Converted to `force-dynamic` redirect → `/pricing#founding-buyer` (兜底) | ✅ REAL |
| `/` (home) | No price copy conflict; Founding Buyer link points to `/pricing#founding-buyer` via shared footer | ✅ REAL |
| Supplier Profile | `membershipHref` → `/pricing#founding-buyer`; `founderCta` copy → Founding Buyer | ✅ REAL |
| `/join-supplier-network` | No `/membership` link; self-canonical retained | ✅ REAL |
| `/factory-audit/request` | Audit-type layering intact; self-canonical retained | ✅ REAL |
| `/services/inspection` | `pricingLead` now $199 in all 9 locales; self-canonical retained | ✅ REAL |
| `/tools/supplier-risk-calculator` | 8-dim copy; algorithm already 8-dim; self-canonical retained | ✅ REAL |

Internal link sweep: **zero** rendered `p("/membership")` links remain (`[6]`). Footer, account menu, suppliers list, register, checkout-return all point at `/pricing#founding-buyer`.

---

## 3. SEO — Canonical / Redirect / Sitemap

### 3.1 Redirect `/membership` → `/pricing` (308) — ✅ REAL
- `next.config.mjs` `redirects()`: `/membership` → `/pricing` and `/:locale/membership` → `/:locale/pricing`, both `permanent: true` (308). (`[5]`)
- Page-level `redirect()` in `app/[locale]/membership/page.tsx` as in-app 兜底 (anchored `#founding-buyer`).
- `docs/REDIRECT-MAP.md` written (Phase 04).

### 3.2 Self-canonical — ✅ REAL
- All 5 core pages (`/pricing`, `/services/inspection`, `/tools/supplier-risk-calculator`, `/factory-audit/request`, `/join-supplier-network`) emit self-canonical via shared layout (`canonicalFor(locale, x-pathname)`). Verified the layout derives canonical from the `x-pathname` header (CS-01 fix) — no per-page gap.

### 3.3 Sitemap — ✅ REAL
- `/membership` removed from `app/sitemap.ts` `core` array (`[4]`).
- `app/llms.txt/route.ts`: `[Founding Buyer](${BASE}/pricing#founding-buyer)` replaces old `[Buyer Membership]` entry.

### 3.4 No "certified suppliers" claim — ✅ REAL
- `lib/seo.ts:28` description changed from "certified suppliers" to neutral "verified factory directory" (spec §54).

---

## 4. Analytics / Regression (Phase 05) — ✅ REAL
- `scripts/v22-commercial-regression.mjs` written: 7 sections, **61 checks, all PASS**.
  - `[1]` commercialConfig truth · `[2]` commerce.ts cents · `[3]` per-locale pricing ranges + no 10-dim copy · `[3b]` inspection page-body $199 · `[4]` sitemap omits /membership · `[5]` next.config redirects · `[6]` no rendered `/membership` links / no public "Verified Supplier" · `[7]` retired membership analytics events.
- `npx tsc --noEmit` → **EXIT 0** (strict mode).
- All 9 dict JSON parse clean.

---

## 5. Build / Deploy / Smoke / Google QA — ⬜ TODO (manual, outside sandbox)

| Item | Status | Note |
| --- | --- | --- |
| `next build` + `opennext build` | ⬜ TODO | Must run with sandbox OFF (per project deploy rule). tsc + regression already green. |
| Cloudflare Workers deploy (`wrangler deploy`) | ⬜ TODO | Four-step deploy: clear `.next/cache` → next build → opennext build → scrub-next-env → deploy. |
| Live 308 check (`/membership` → `/pricing`) | ⬜ TODO | Verify post-deploy via `curl -I`. |
| Canonical self-reference per core page | ⬜ TODO | Verify in rendered `<head>` post-deploy. |
| Google Search Console URL inspection | ⬜ TODO | Confirm /membership dropped, /pricing indexed. |
| `npm install` for new deps | 🔲 N/A | No new runtime deps added; all changes are source-only. |

> Note: `npm` is blocked by the sandbox Security Center (`wsl.exe` shim); the full production build/deploy must be executed by the user (or in Agent mode with sandbox disabled) per the project's deploy discipline. No code change is blocked by this — typecheck and the consistency regression already pass.

---

## 6. Residual notes / follow-ups
- **`scripts/*.cjs` migration files** still contain legacy "Founder Buyer" strings — these are one-time dev migration artifacts, not runtime. Left as-is (editing them has zero production effect). If strict repo cleanliness is wanted, a follow-up pass can normalize them.
- **`lib/supplierNetwork.ts`** internal 10-dim model is intentionally retained (spec §27/§68 — do not break historical assessments). It is not the public risk model, so the 8-dim public rule is unaffected.
- **`membership` dict key** remains the structural key name in all 9 locales (its display value is now "Founding Buyer"/localized). The key name itself was not renamed to avoid component churn; display copy is correct.

---

## 7. Sign-off
- Commercial rules unified to a single truth source: ✅
- Inspection = $199 across price table, meta description, and inspection page body (9 locales): ✅
- Risk model uniform 8-dimension in code + copy: ✅
- `/membership` → `/pricing#founding-buyer` 308 + sitemap/llms.txt cleanup: ✅
- Typecheck + 61-check regression green: ✅
- Production build/deploy + live Google QA: ⬜ pending manual deploy (sandbox-off)
