# FactoryAuditB2B 上线部署清单

**更新日期**：2026-08-31
**当前状态**：代码已具备上线资格 · 生产环境剩 2 项待你决策

---

## 一、已完成（本次会话）

| 项目 | 状态 | 验证方式 |
|---|---|---|
| `AUTH_SECRET` 会话密钥 | 已确认正常（44 字符） | cookie 为 5 段 JWE，篡改后访问 `/admin/leads` 返回 307 |
| `/api/lead` 限流 | 同 IP 5 条/小时 | 实测放行 5 / 拦截 3 |
| `/api/register` 限流 | 同 IP 5 个/小时 | 实测放行 5 / 拦截 2 |
| 输入校验与长度截断 | message 上限 5000 | 输入 20000 字符 → 落库 5000 |
| 限流按 IP 隔离 | 不误伤正常用户 | 8 个独立 IP 全部放行 |
| SMTP 配置模板 | 已加服务商速查表 | 见 `.env` 邮件段 |
| 9 语言限流文案 | 已补齐 | `auth.errors.rate_limited` |

---

## 二、待你决策（阻塞项）

### 1. 提供真实 SMTP —— 最高优先级

**为什么阻塞**：现在是 Ethereal 测试账号。邮件会被 SMTP 接受并返回 messageId，但**不投递到任何真实收件箱**。客户提交询价后收不到确认邮件，你也收不到线索通知——双方都在空等。你是靠「先跑询盘」冷启动的，询盘静默丢失等于零转化。

**给我这 4 个值即可**（替换 `.env` 里 SMTP 段的 4 行）：

```
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
```

**服务商速查**（已在 `.env` 注释里）：

| 服务商 | HOST | PORT | USER | PASS |
|---|---|---|---|---|
| 腾讯企业邮 | smtp.exmail.qq.com | 465 | 完整邮箱 | 密码或客户端专用密码 |
| 阿里云邮 | smtp.qiye.aliyun.com | 465 | 完整邮箱 | 密码 |
| QQ 邮箱 | smtp.qq.com | 465 | 完整邮箱 | **授权码**（非登录密码） |
| SendGrid | smtp.sendgrid.net | 587 | `apikey` | SG. 开头的 API Key |
| Gmail | smtp.gmail.com | 587 | 完整邮箱 | 应用专用密码 |

> 465 走 SSL，587 走 STARTTLS，代码已按 `SMTP_PORT` 自动切换。
> 建议 `FROM_EMAIL` 用你自己的域名邮箱，否则容易被判垃圾邮件。

### 2. 定域名

四个候选：

| 域名 | 特点 |
|---|---|
| verifyb2b.com | 短、动词化，但 .com 可能已被注 |
| FactoryAuditB2B.com | 品牌一致，偏长 |
| asiafactoryverify.com | 地域清晰，含 verify |
| cnfactoryaudit.com | 直白，但 cn 开头限定了地域想象 |

**建议**：`verifyb2b.com` 如果还能注册就选它——短、好记、不限定地域（你已经在做越南/泰国/马来/菲律宾），对未来扩品类也友好。

### 3. 决定部署形态 —— 这决定要不要先迁 PostgreSQL

| 部署形态 | SQLite 能否用 | 说明 |
|---|---|---|
| **云主机**（腾讯云 CVM / LightHouse，有持久磁盘） | **可以** | 你当前 DB 只有 268K，流量也低，SQLite 完全够用，**不必先迁库** |
| **Serverless**（CloudBase / EdgeOne） | **不行** | 文件系统不持久，重启即丢数据，**必须先迁 PostgreSQL** |

**我的建议**：先用云主机。你现在的量级（268K 数据、零到低流量）迁库是纯粹的额外风险，等有真实订单量再迁不迟。

---

## 三、部署当天要做的（定完上面 3 项后，约半天）

1. 买域名 + 解析到服务器 IP
2. 服务器上拉代码、`npm ci`
3. 写生产 `.env`：
   - `NEXTAUTH_URL="https://你的域名"` ← **必须改**，否则 OAuth 回调和 session 会出错
   - `AUTH_SECRET` 用新生成的（不要沿用开发的）
   - 真实 SMTP 四件套
   - `DATABASE_URL` 保持 SQLite 或改成 Postgres
4. `npx prisma generate` + `npx prisma db push`（如迁 Postgres）
5. `npm run build` → `npm run start`（或用 PM2 守护）
6. 配 HTTPS（Let's Encrypt / 平台自带证书）
7. 改管理员密码（`FactoryAudit2026!` 不能再用了）

### 构建命令（Windows 必须，否则 EPERM 报错）

```bash
export NODE_OPTIONS=""
export HOMEDRIVE=F: HOMEPATH='\wb_home' USERPROFILE='F:\wb_home'
export APPDATA='F:\wb_home\AppData\Roaming' LOCALAPPDATA='F:\wb_home\AppData\Local'
export HOME='F:\wb_home' TEMP='F:\wb_home\temp' TMP='F:\wb_home\temp' TMPDIR='F:\wb_home\temp'
npx next build
```

---

## 四、上线后第一周该盯的

- 每天看 `/admin/leads`，确认线索真的进来了（这是唯一能验证 SMTP 是否真通的方法）
- 提交一个测试询价，看邮箱是否收到通知
- 装 Google Search Console + 提交 sitemap（`/sitemap.xml` 已就绪）
- 装 GA 或同等分析（目前未接）

---

## 五、可以往后放的（不影响获客型上线）

| 项目 | 何时做 |
|---|---|
| 在线支付（Stripe/Alipay） | 有第一笔需要线上收的款时 |
| SQLite → PostgreSQL | 线索量过千或要上 Serverless 时 |
| Google OAuth | 发现注册转化率低时 |
| 补供应商/行业内容 | SEO 起量阶段持续做 |
| DeepSeek API Key | 想让 AI 分析从规则引擎升级为真 AI 时 |
