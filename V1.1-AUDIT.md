# FactoryAuditB2B V1.1 审计报告（Audit Only，未改任何代码）

**审计日期**：2026-09-01　**站点**：https://factoryauditb2b.com
**审计范围**：现有代码 + 线上站点　**结论**：**不需要推倒重做**，V1.1 可在现有架构上 extend 实现

> 说明：此前完成的是 **V2.0 架构层**（去数据库、去登录/注册/admin、线索邮件化、Cloudflare 部署）。
> 本次 **V1.1 是产品与定位层升级**（Supplier Risk Intelligence Platform），两者不冲突，代码继续基于现有 V2.0 架构扩展。

---

## 1. 当前架构（Current Architecture）

| 项 | 现状 |
|---|---|
| 框架 | Next.js 15.5（App Router）+ React 19 + TypeScript strict |
| 样式 | Tailwind v4（globals.css 自定义 .btn/.card/.badge/.container 等） |
| 数据 | **无数据库**（V2.0 已移除 Prisma/Neon），全走静态 TS 常量 `lib/staticData.ts` |
| 国际化 | 9 语言（en 默认无前缀 + zh/es/de/fr/pt/ja/zh-TW/ar 带前缀），字典 `i18n/dictionaries/*.json`，类型以 en.json 为基准 |
| AI | DeepSeek（`@ai-sdk/openai-compatible`），无 key 时自动降级本地规则引擎 |
| 部署 | Cloudflare Workers（OpenNext），Worker 版本 `cb9908e4` |
| 支付 | 无（V2.0 已停用），付费 CTA 导向 /custom-services 询价 |
| 线索 | `/api/lead` → `crypto.randomUUID()` + 邮件双通道（SMTP / HTTP）通知 |

---

## 2. 当前页面（Current Pages）— 39 个 page.tsx

| 类别 | 路由 |
|---|---|
| 首页 | `/` |
| 工具（7 页面 + 索引） | `/tools`、`supplier-risk-calculator`、`supplier-risk-assessment`、`supplier-scorecard`、`supplier-verification-checklist`、`supplier-document-checker`、`audit-checklist`、`audit-report-analyzer` |
| 服务（4） | `/services`、`/services/supplier-verification`、`/services/inspection`、`/services/supplier-improvement` |
| 国家/地区 | `/countries`、`/countries/[slug]`、`/country/[slug]` |
| 行业 | `/industry`、`/industry/[slug]` |
| 审核指南 | `/audit-guide/[country]/[auditType]`（仅 Phase 1 三国） |
| 供应商 | `/suppliers`、`/supplier/[country]/[slug]` |
| 内容 | `/guides`、`/guides/[slug]`、`/case-studies`、`/case-studies/[slug]`、`/resources`、`/knowledge` |
| 商业 | `/pricing`、`/rfq`、`/custom-services`、`/factory-audit/request`、`/training-plans` |
| 信任 | `/trust`（304 行）、`/methodology`（157 行）、`/about`、`/inspectors` |
| 法务/其他 | `/privacy`、`/terms`、`/logistics` |

---

## 3. 当前组件（Current Components）— 13 个 + tools/3

| 组件 | 职责 |
|---|---|
| `HeroSearch.tsx` | Hero 搜索框（**4 个 tab**，本地过滤，不提交后端） |
| `RfqForm.tsx` | RFQ 表单 |
| `AuditRequestForm.tsx` | 验厂申请表单 |
| `InspectionRequestForm.tsx` | 验货申请表单 |
| `CustomServiceForm.tsx` | 定制服务询价表单 |
| `AiChatWidget.tsx` | AI 客服（DeepSeek + 本地 FAQ 降级） |
| `JsonLd.tsx` | 结构化数据注入 |
| `WatermarkedDocument.tsx` | **加水印公开证件图**（信任机制，非 Sample Report） |
| `WhatsAppLink.tsx` | WhatsApp 联系按钮 |
| `SiteHeader/SiteFooter/LocaleSwitcher` | 导航/页脚/语言切换 |
| `tools/` | `SupplierRiskCalculator`、`SupplierVerificationChecklist`、`ContainerLoadCalculator` |

---

## 4. 当前数据层（Current Data）— 无数据库

| 文件 | 内容 |
|---|---|
| `lib/staticData.ts` | 静态常量：9 国 / 12 行业 / 22 审核认证 / 15 类目 / **4 个供应商** |
| `lib/riskEngine.ts` | **6 维风险引擎**（company 15 / quality 20 / compliance 20 / production 15 / supplychain 15 / documentation 15） |
| `lib/coverage.ts` | 9 国差异化内容 + verification/audit 服务内容 |
| `lib/guides.ts` | 10 篇指南（en/zh 手写，其余回退 en） |
| `lib/caseStudies.ts` | **5 个案例**（已满足 V1.1「3–5 个高质量案例」要求） |
| `lib/verification.ts` | 核验等级 Level 0–4、证据状态/类型标签 |
| `lib/trust.ts` | 主体信息（营业执照/注册机关/注册地址，支持脱敏） |

⚠️ **供应商数据仅 4 条**。但 V1.1 明确要求「不追求数量、重点做 Supplier Trust Data」，因此**不构成阻塞**，反而与定位一致。

---

## 5. 当前 API（Current APIs）— 5 个

| 路由 | 用途 |
|---|---|
| `/api/lead` | 线索提交（UUID + 邮件通知） |
| `/api/risk` | 风险评分（AI 或本地规则引擎） |
| `/api/chat` | AI 客服 |
| `/api/rfq-draft` | RFQ 草稿生成 |
| `/api/report-analyze` | 报告分析 |

---

## 6. 当前 SEO（Current SEO）

| 项 | 现状 |
|---|---|
| Metadata | `lib/pageMeta.ts` 的 `buildPageMetadata()` 统一注入（title/desc/canonical/OG/Twitter） |
| Sitemap | `app/sitemap.ts`：8 tools + 4 services + 信任页 + 国家/行业/指南/案例等 |
| Robots | `app/robots.ts`（disallow 收缩到 `/api`） |
| AI 收录 | `app/llms.txt/route.ts` |
| 结构化数据 | Organization + WebSite + ItemList + Service + FAQPage + BreadcrumbList + Article |
| hreflang | 9 语言互链 |
| OG 图 | `public/og-image.png`（1200×630），常量 `OG_IMAGE` |

**SEO 基础扎实，V1.1 不需要动 URL 结构**（符合「不破坏现有 SEO URL」要求）。

---

## 7. 当前表单（Current Forms）— 4 个

`RfqForm` / `AuditRequestForm` / `InspectionRequestForm` / `CustomServiceForm`
→ 全部提交到 `/api/lead` → 生成 leadId → 邮件双通道通知 → 人工跟进。
**无 CRM、无支付、无客户后台**（V1.1 第 34 条明确要求保持此状态）。

---

## 8. 当前转化漏斗（Current Conversion Funnel）

```
Google / 直接访问
      ↓
首页 Hero（3 个 CTA：Assess Supplier Risk / Verify a Supplier / Request a Factory Audit）
      ↓
免费工具（4 个）或 服务页
      ↓
表单（RFQ / 验厂 / 验货 / 定制）
      ↓
邮件 → 人工跟进 → 线下成交
```

**漏斗断点**：
1. Hero 搜索框**只本地过滤 4 个供应商**，用户输入任意公司名 → 显示「no match」，**既不返回价值也不收集线索**（最大流失点）
2. 工具给完分数后，没有「下一步是什么」的强引导（无 Sample Report、无透明定价）
3. 无 RiskScore 品牌，4 个工具各叫各的名字，用户记不住

---

## 9. 当前问题（Current Problems）— 按严重度

### 🔴 P0 阻塞（不做则 V1.1 定位无法成立）

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| 1 | **评分方向与 V1.1 相反** | `lib/riskEngine.ts`：`overallLevel()` 中 `score<=20=LOW` … `>80=CRITICAL`，即**分数越高=风险越高**。V1.1 要求 72/100 = MEDIUM RISK，即**分数越高=风险越低** | 引擎 + 所有工具页 + 9 语言字典文案全部要反 |
| 2 | **6 维 ≠ 8 维** | 现有 6 维（company/quality/compliance/production/supplychain/documentation）。V1.1 要求 8 维，**缺 Certification 与 Digital Footprint** | 引擎结构 + 题库 + 结果页 |
| 3 | **Sample Report 完全缺失** | 全库 `sample-report` / `sampleReport` 命中 **0 个文件** | P0 #6 无法交付 |
| 4 | **Hero 文案与 CTA 未统一** | 现 `ctaPrimary="Assess Supplier Risk"`、`ctaSecondary="Verify a Supplier"`、`ctaHighIntent="Request a Factory Audit"`。V1.1 要求统一为 Check My Supplier / Free Risk Check | 9 语言字典 + 首页 |
| 5 | **Hero 输入框不收集线索** | `HeroSearch.tsx` 本地过滤，无后端提交，无 URL 解析 | 最大转化流失点 |
| 6 | **定价与 V1.1 差距大** | 现有 $0 / **$29** / **$79–$149** / Custom quote。V1.1 要求 Free $0 / Verification **$99–129** / Audit **$399+** / Inspection **$99+** / Monitoring 订阅 | 需用户确认是否接受涨价 |

### 🟡 P1 缺失（V1.1 明确要求，目前没有）

| # | 缺失项 | 证据 |
|---|---|---|
| 7 | **Supplier Comparison** | `Compare` 仅出现在 lib 文案中，**无对比功能** |
| 8 | **Supplier Monitoring** | `Monitor` 命中 0 个文件 |
| 9 | **Field Reports** | `field-report` / `fieldReport` 命中 **0 个文件** |
| 10 | **Claim Profile** | 无（P2，暂缓） |
| 11 | **Evidence 展示层** | `lib/verification.ts` 已有证据模型（Level 0–4），**但未在工具结果页展示 Evidence/Source/Checked Date/Confidence** |
| 12 | **Human + AI 定位、6 条验证原则** | `/methodology`（157 行）与 `/trust`（304 行）已有基础，但缺 Human+AI 分工表述与「Never Hide Uncertainty」6 原则 |

### 🟢 已满足（无需新做）

| V1.1 要求 | 现状 |
|---|---|
| Case Studies 3–5 个 | ✅ `lib/caseStudies.ts` 已有 **5 个** |
| Trust / How We Verify | ✅ `/methodology` + `/trust` 已存在，仅需增强 |
| 免费工具作为 SEO 入口 | ✅ 7 个工具页已在 sitemap |
| 不伪造数据 / 区分 Verified-Not Verified | ✅ V4.0 已定「禁止 Download」「无记录不显示 Verified」铁律 |
| 无数据库 / Google Sheets 闭环 | ✅ V2.0 已完成 |
| 移动端友好 | ✅ Tailwind 响应式（需复核 RiskScore 结果页） |

---

## 10. V1.1 建议改动（Proposed Changes）— 文件级清单

> 原则：**extend 而非 rewrite**；不删除任何现有页面/URL/工具/内容。

### P0 — 必须优先（8 项）

| # | 改动 | 涉及文件 | 为什么改 | 解决什么 | 影响 SEO | 影响现有功能 |
|---|---|---|---|---|---|---|
| P0-1 | Hero 重定位：文案 `Know Your Supplier Before You Risk Your Money` + CTA `Check My Supplier` / `Free Risk Check` | `i18n/dictionaries/*.json`（9 份 home.h1/lead/sub/cta*）、`app/[locale]/page.tsx`（L100–126） | 现有文案未传达「付款前查供应商」的决策场景 | 5 秒内说清我是谁/解决什么/现在能做什么 | **无**（URL 不变，title/desc 随字典更新，有利） | 无（纯文案 + 按钮链接） |
| P0-2 | Hero 输入框升级：支持公司名/网站/Alibaba/1688 URL，未命中时引导「Submit for preliminary assessment」 | `components/HeroSearch.tsx`（新建提交分支）、`app/api/lead/route.ts`（复用，新增 source 字段）、字典新增 key | 现只能匹配 4 个已收录供应商，用户输入任意名即 no-match，零线索回收 | 把 Hero 从「搜索框」变成**主要线索入口** | 无 | 低（复用现有 lead API，不动既有字段） |
| P0-3 | RiskScore™ 品牌统一：4 个工具纳为同一品牌 | `lib/nav.ts`（TOOL_ORDER）、字典 toolCards、4 个工具页 meta | 4 个工具各叫各的名字，用户无品牌记忆 | 建立 FactoryAuditB2B RiskScore™ 心智 | **低风险**（保留原 URL 做 canonical，新增品牌名不换 slug） | 无 |
| P0-4 | 风险引擎反转 + 6→8 维 | `lib/riskEngine.ts`（`overallLevel()` 分桶反转、DIMENSION_STRUCTURE 加 certification / digitalFootprint 并调权重）、字典新增维度文案 | V1.1 要求 72/100=MEDIUM（高分=低风险），现有相反；缺 2 个维度 | 分数可读、维度覆盖 V1.1 全 8 项 | 无（工具页 URL 不变） | **中**（工具结果数值变化，需同步改结果页展示与字典） |
| P0-5 | RiskScore 结果页：分数 + 等级 + 8 维明细 + Key Findings + Recommendation + CTA | `components/tools/SupplierRiskCalculator.tsx`、新增 `components/tools/RiskScoreResult.tsx`、字典 | 现只有分数 + 6 维条，无 Findings/Recommendation/Evidence | 「为什么这个分数」可解释，形成 Check→Score→Verify 闭环 | **正面**（可加 FAQPage schema） | 中（替换结果区组件） |
| P0-6 | **Sample Report 页**（完全新增） | 新增 `app/[locale]/sample-report/page.tsx` + 字典；复用 `WatermarkedDocument` 思路 | 全库 0 命中，客户买前看不到交付物 | 提高付费转化 | **正面**（新 SEO 入口 + Article/DigitalDocument schema） | 无（纯新增） |
| P0-7 | Pricing 重构：$0 / Verification $99–129 / Audit $399+ / Inspection $99+ / Monitoring 订阅 | `app/[locale]/pricing/page.tsx`、`i18n/dictionaries/*.json`（pricing.plans） | 现有 $29/$79–149 与 V1.1 定位不符 | 价格透明、锚定价值 | 无 | 无（价格仅展示，无支付逻辑） |
| P0-8 | Trust 增强：Human + AI 分工 + 6 条验证原则 + How We Verify 六步 | `app/[locale]/trust/page.tsx`（304 行）、`app/[locale]/methodology/page.tsx`（157 行）、字典 | 缺 Human+AI 与「Never Hide Uncertainty」 | 建立公信力与差异化 | **正面**（可补 FAQPage） | 无（在原页追加区块） |

### P1 — 第二阶段（6 项）

| # | 改动 | 涉及文件 |
|---|---|---|
| P1-9 | Supplier Comparison（2–5 家对比 + 免责声明） | 新增 `/tools/compare` 或 `/compare`；复用 riskEngine；字典 |
| P1-10 | Supplier Profile 升级（Risk Score / Verification Status / Last Checked） | `app/[locale]/supplier/[country]/[slug]/page.tsx`、`lib/queries.ts` |
| P1-11 | Case Studies 页面化（已有 5 条数据，接入 Problem→Investigation→Findings→Risk Score→Recommendation 结构） | `app/[locale]/case-studies/`、`lib/caseStudies.ts` |
| P1-12 | Field Reports（新增，脱敏发布） | 新增 `/field-reports`、`/field-reports/[slug]`；字典 |
| P1-13 | AI Risk-Based Audit（RiskScore → 自动推荐审核范围） | `app/[locale]/factory-audit/request/page.tsx`、`lib/ai.ts` |
| P1-14 | Supplier Monitoring（订阅制展示 + 风险变更提示概念） | 新增 `/monitoring` 或 Pricing 内区块；字典 |

### P2 — 暂缓（按用户第 47 条）

Claim Profile、Risk Alerts 自动化、更多国家、更多行业 Landing、企业 Dashboard、API/Data Platform。

---

## 11. 需要用户拍板的 4 个决策点

| # | 决策点 | 选项 | 我的建议 |
|---|---|---|---|
| 1 | **评分方向反转**是否确认？ | A. 反转为「高分=低风险」（符合 V1.1 文档）<br>B. 保持「高分=高风险」，只改文案 | **A**（V1.1 文档明确 72/100=MEDIUM）。但要注意：现有老用户/已外链的分数含义会变，需全站一次性改完 |
| 2 | **定价是否接受大幅上调**？ | A. 按 V1.1：Verification $99–129、Audit $399+<br>B. 保守过渡：$49–79 / $299+<br>C. 维持现状 | **A**，但建议定价页同时保留「按项目报价」入口，避免吓跑小单 |
| 3 | **P0 是否需要 8 个全做**，还是先做前 5 个（Hero + RiskScore）？ | A. 8 个全做<br>B. 先做 P0-1~5（定位 + RiskScore），Sample Report/Pricing/Trust 放第二批 | **B**（先让核心路径跑通：Hero→Check→Score→Verify，再补交付物展示与定价） |
| 4 | **Hero 输入框后端如何处理**？ | A. 只收线索（进 /api/lead，人工跟进）<br>B. 线索 + AI 初筛（DeepSeek 做公开信息初筛，标注「AI 初筛，非核验结论」） | **A 起步**（V1.1 第 7 条允许「先实现 Submit for preliminary assessment」），B 放 P1 |

---

## 12. 风险提示

1. **引擎反转是本次最大改动面**：`lib/riskEngine.ts` 的 `overallLevel()` + 6 维 → 8 维 + 4 个工具页结果展示 + 9 语言字典，建议一次性改完并跑全站回归，避免中英文站分数含义不一致。
2. **字典改动量大**：V1.1 新增大量文案（Hero、8 维度、Findings、Recommendation、Sample Report、Pricing、Trust 原则），需按项目铁律**先改 en.json，再用 `scripts/sync-dict.cjs` 同步其余 8 语言**。
3. **禁止伪造数据**：Sample Report 与 Case Studies 必须标注 Sample / Demonstration，沿用 V4.0 铁律（无记录不显示 Verified、禁止 Download）。
4. **供应商仅 4 条**：Hero 搜索命中率天然低，必须通过「未命中即引导提交」兜住线索，否则 Hero 输入框形同虚设。
